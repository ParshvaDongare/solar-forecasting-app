This folder contains files deprecated when converting the project to Streamlit.
Files moved here:
- api.py (Flask backend)
- templates/ (Flask HTML templates)
- static/ (Flask static assets)
- vercel.json (Vercel config)
- .vercelignore (Vercel ignore)
- Procfile (Heroku)

Keep this folder only if you want to restore the Flask-based setup later.
